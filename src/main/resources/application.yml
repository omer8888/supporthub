spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supporthub?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
app:
  jwt:
    secret: ${JWT_SECRET:change-me-in-prod-this-must-be-at-least-32-bytes-long!!}
    ttl-minutes: 60
